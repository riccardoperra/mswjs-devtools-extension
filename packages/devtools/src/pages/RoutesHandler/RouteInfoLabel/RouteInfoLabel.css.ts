import { style } from "@vanilla-extract/css";

export const divider = style({
  marginLeft: "8px",
  marginRight: "8px",
  fontWeight: 600,
  userSelect: "none",
});

export const label = style({
  fontWeight: 600,
});
